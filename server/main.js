import { Meteor } from 'meteor/meteor';
import { Accounts } from 'meteor/accounts-base'
import './publish'

Meteor.startup(() => {
  // code to run on server at startup
  

  // Accounts.createUser(userInfo2, function (err) {
  //   if (err) {
  //     console.log(err)
  //   }
  // })
});
